import { css } from '@emotion/react';

export default function Content() {
  return (
    <div
      css={css({
        backgroundColor: 'green',
        color: 'white',
      })}>
      profile
    </div>
  );
}
